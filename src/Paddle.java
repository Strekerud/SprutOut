
public class Paddle {
	
	int width;
	int height;
	
	double x_pos;
	double y_pos;
	
	Paddle(int width, int height, double x_pos, double y_pos) {
		this.width = width;
		this.height = height;
		this.x_pos = x_pos;
		this.y_pos = y_pos;
	}
}
